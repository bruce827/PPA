> 我现在说的所有功能都在新建评估页面，第一步，加一个“一键评估”功能，点击后用户需要输入这个项目的招标文件，或是其它要求（文本格式 │
│    ），然后选择应用的提示词（数据来源在模型配置/提示词管理模块）并配置提示词中的变量，点击评估后，系统会根据提示词和上下文（用户  │
│    输入的文本），调用当前使用的模型（模型信息在模型配置/模型管理中）自动评估当前风险项（风险项在参数配置/风险项管理中）的评分，并 │
│    给出可能缺失的风险项及建议，也就是说，系统再模型调用成功后，需要自动填充我这个页面第一步的表单，并给出建议（文本），这是这个页 │
│    面第一步的功能，你先理解并优化design，然后我再说第二步的功能  




create-story 根据这个prd创建用户故事，命名应该是ai-1-2**,因为这个ai-1-1的后端实现