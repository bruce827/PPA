draft_id: "draft-20260320-000000"
generated_at: "2026-03-20T16:35:00+08:00"

analysis_type: "distribution"
sql_query: |
  SELECT
    project_type,
    COUNT(*) as count,
    AVG(final_total_cost) as avg_cost,
    MIN(final_total_cost) as min_cost,
    MAX(final_total_cost) as max_cost
  FROM projects
  WHERE final_total_cost IS NOT NULL
  GROUP BY project_type
  ORDER BY count DESC;

data_summary:
  row_count: 2
  column_count: 5
  columns: ["project_type", "count", "avg_cost", "min_cost", "max_cost"]

data_preview: |
  project_type,count,avg_cost,min_cost,max_cost
  standard,35,213.43,21.0,716.0
  web3d,6,60.75,30.7,150.82

export_path: "./outputs/db-data-draft-20260320-000000.csv"
