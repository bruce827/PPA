# 模式 D 完整流程测试报告

**测试日期**: 2026-03-23  
**测试人**: Agent  
**测试目标**: 验证 Owner 引导模式完整流程无死路

---

## 测试用例

### 用例 1: 基础 Count 查询

**用户问题**: "招标文件一共推送了多少？"

**执行步骤**:
1. `[OWNER]` - 入口，识别意图类型 count
2. `[CLARIFY]` - 澄清时间范围（全部时间）、推送状态（已推送）
3. `[DATA_MAP]` - 映射到表 opportunity_tender_staging，生成 SQL
4. `[QUERY_BUILD]` - 执行查询，返回结果 5
5. `[REPORT_GEN]` - 生成轻量级报告

**结果**: ✅ 通过

---

### 用例 2: 分布分析查询（新意图类型）

**用户问题**: "各渠道的招标数量分布如何？"

**意图类型**: distribution（v1.1 新增）

**预期 SQL**:
```sql
SELECT source_platform, COUNT(*) as count
FROM opportunity_tender_staging
GROUP BY source_platform
ORDER BY count DESC
```

**预期结果**:
| source_platform | count |
|-----------------|-------|
| 国家管网电子招标平台 | 80 |
| 中国石油招标投标网 | 20 |
| 中国海洋石油采办业务管理与交易系统 | 20 |

**状态**: 🟡 待完整执行（需更新 owner-data-mapper.md 支持 distribution 类型）

---

### 用例 3: 排名分析查询（新意图类型）

**用户问题**: "推送状态排名前几的是哪些？"

**意图类型**: ranking（v1.1 新增）

**预期 SQL**:
```sql
SELECT push_status, COUNT(*) as count
FROM opportunity_tender_staging
GROUP BY push_status
ORDER BY count DESC
LIMIT 5
```

**状态**: 🟡 待完整执行

---

## 流程监控点

| 步骤 | 前置检查 | 产物文件 | 状态更新 | 可监控 |
|------|----------|----------|----------|--------|
| OWNER_ENTRY | 无 | owner-intent-{draft_id}.yaml | workflow_mode=owner-guided | ✅ |
| INTENT_CLARIFY | OWNER_ENTRY | owner-intent-{draft_id}.yaml (updated) | current_step=INTENT_CLARIFY | ✅ |
| DATA_MAP | INTENT_CLARIFY | owner-data-map-{draft_id}.yaml | current_step=DATA_MAP | ✅ |
| QUERY_BUILD | DATA_MAP | owner-query-result-{draft_id}.yaml | current_step=QUERY_BUILD | ✅ |
| REPORT_GEN | QUERY_BUILD | owner-report-{level}-{draft_id}.md | current_step=DONE | ✅ |
| FOLLOWUP | REPORT_GEN | 新增产物 | 保持 DONE 或新增轮次 | ⚠️ 需实现 |

---

## 发现的问题

### 1. 意图类型枚举未同步更新

**文件**: `owner-branch-entry.md` 和 `owner-intent-clarify.md`

**问题**: 仍使用旧版 4 种意图类型（count/comparison/trend/diagnosis），缺少 v1.1 新增的：
- distribution（分布分析）
- ranking（排名分析）
- correlation（相关性分析）

**建议**: 更新这两个文件，同步 `workflow-contract.md` 的枚举定义

---

### 2. FOLLOWUP 步骤缺少实现细节

**文件**: `owner-followup.md`

**问题**: 仅定义了追问类型，但未明确：
- 如何更新 `workflow-state.json`（是否新增轮次记录）
- 追问产物如何命名
- 如何保持上下文（followup_context 结构）

**建议**: 补充 FOLLOWUP 执行规范

---

### 3. DATA_MAP 的 security_check 未实际执行

**文件**: `owner-data-mapper.md`

**问题**: 虽然定义了 `security_check` 字段，但没有实际的验证逻辑代码

**建议**: 添加 SQL 验证函数实现

---

## 产物文件清单

测试产生的文件：
- `outputs/owner-intent-draft-20260323-000000.yaml`
- `outputs/owner-data-map-draft-20260323-000000.yaml`
- `outputs/owner-query-result-draft-20260323-000000.yaml`
- `outputs/owner-report-lite-draft-20260323-000000.md`

---

## 结论

**基础流程**: ✅ 无死路，每步可监控  
**新意图类型**: ⚠️ 需更新参考文档  
**多轮追问**: ⚠️ 需补充实现细节

**整体评价**: 模式 D 主干流程完整，v1.1 新增功能需补充执行细节
